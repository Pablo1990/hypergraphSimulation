#Developed by Pablo Vicente Munuera
files <- list.files(path = '../data', full.names = T)

hg <- file(files[1], open = 'rt')

lines <- readLines(hg)

hyperedgeList <- list()

for (line in lines){
  
}

